#!/usr/bin/env bash
# scripts/ci.sh — project ごとに実装。default は no-op。
# Replace this with: linting / unit tests / build verification etc.
set -euo pipefail
echo "[ci] no-op (replace with project-specific tests)"
exit 0
