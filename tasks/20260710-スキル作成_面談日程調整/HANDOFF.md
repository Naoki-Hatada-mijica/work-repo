# HANDOFF — 面談日程調整スキル

## いまどこ（2026-07-10 時点）
「先にカレンダー連携をセットアップ → 全自動を目指す」方針（フェーズB）で進行中。
**カレンダー連携と対応可能日時の抽出エンジンまで完成・動作確認済み。** 次は「書き込み」から。

## 完了済み
- タスク一式（PLAN/SPEC/TODO/KNOWLEDGE/input）作成
- Google カレンダー連携（案A: API 連携）セットアップ・動作確認
  - GCP プロジェクト `interview-scheduler-502002`、Calendar API 有効、OAuth=内部、デスクトップアプリ
  - 秘密情報は `~/.config/interview-scheduler/`（client_secret.json / token.json / venv）＝リポジトリ外
  - スコープ: `https://www.googleapis.com/auth/calendar`
- `scripts/gcal_match.py` — 候補ウィンドウ(JSON) → 平日10-20時・空き・1時間以上の枠を抽出し
  「▼面談可能日時」フォーマットで出力。実カレンダーで検証済み。

## 確定した仕様
- 営業時間: 平日 10:00〜20:00（土日祝除外）
- 標準所要時間: 1時間 / 確定枠は前後30分バッファ
- 突合: 候補の幅をそのまま返す（連続空き帯を ○:○○〜○:○○）
- 出力フォーマット・確定メモ欄フォーマット・アウトプットは SPEC.md 参照

## 次にやること（優先順）
1. 面談調整ブロックの自動登録（書き込み）
   - タイトル `面談調整｜{クライアント} × {候補者}`、対応可能枠に予定作成
2. フェーズ2（確定処理）
   - 確定枠を1時間ブロック `【面談 / WEB】{クライアント} × {候補者}` ＋前後30分バッファ
   - メモ欄に確定フォーマット（SPEC.md 4節）記入
   - 同クライアント×候補者の「面談調整」ブロックを全削除（削除は対象列挙して確認）
   - 候補者向け「面談確定フォーマット」を整形出力
3. 一連を SKILL.md 化（.claude/skills/interview-schedule-coordination/）

## 注意・ハマりどころ（詳細は KNOWLEDGE.md）
- 認証JSONは作成時ポップアップでしかDL不可。自動化タブではDLブロックされる→ユーザーの通常タブで作成→DL。
- freebusy は UTC 'Z' 表記で返る→JST正規化が必要（対応済み）。
- 削除はユーザーCLAUDE.mdルールで「対象列挙→確認してから」。
- 秘密情報は絶対コミットしない（~/.config 配下で管理）。
